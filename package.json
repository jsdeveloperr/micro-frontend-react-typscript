{
  "name": "micro-frontend-react-typscript",
  "version": "1.0.0",
  "description": "micro frontend reactjs and typscript",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/**"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean",
    "prepare": "lerna run yarn"
  },
  "author": "Abdulnasır olcan",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
